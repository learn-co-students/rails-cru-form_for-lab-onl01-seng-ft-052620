<%= form_for(artist) do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div><br>
  <div>
    <%= f.label :bio %> <br>
    <%= f.text_area :bio %>
  </div><br>
  <%= f.submit button_name %>
<% end %>
