<% @song.each do |song| %>
  <p><%= link_to song.name, song_path(song) %></p>
  <p><%= link_to song.artist_id, artist_path(song.artist) %></p>
  <p><%= link_to song.genre_id, genre_path(song.genre) %></p>
<% end %>